package com.home_27_02_23;

import java.util.Arrays;  /// импортировать класс Arrays из пакета java.util
import java.util.Scanner; /// импортировать класс Scanner из пакета java.util.

public class Task_8_18_Methods_2 { /// объявить открытый класс с именем ask_8_18_Methods_2

    public static void main(String[] args) {  // объявить открытый статический метод main с массивом строк args в качестве аргумента.
        Scanner input = new Scanner(System.in);  // создать объект класса Scanner с именем input и связать его со стандартным потоком ввода.
        System.out.println("Enter the number of rows: "); // вывести на экран приглашение для ввода количества строк.
        int rows = input.nextInt(); // считать целочисленное значение, введенное пользователем, и сохранить его в переменной rows
        System.out.println("Enter the number of columns: "); ///  вывести на экран приглашение для ввода количества столбцов.
        int cols = input.nextInt(); /// считать целочисленное значение, введенное пользователем, и сохранить его в переменной cols
        int[][] matrix = new int[rows][cols]; /// создать новый двумерный массив целых чисел с количеством строк rows и количеством столбцов cols и сохранить его в переменной matrix.
        initialize(matrix); // вызвать метод initialize и передать ему массив matrix в качестве аргумента.
        System.out.println("Original matrix:"); //  вывести на экран сообщение "Original matrix:".
        print(matrix); /// вызвать метод print и передать ему массив matrix в качестве аргумента.
        shuffle(matrix); /// вызвать метод shuffle и передать ему массив matrix в качестве аргумента.
        System.out.println("Shuffled matrix:"); /// вывести на экран сообщение "Shuffled matrix:".
        print(matrix); // ызвать метод print и передать ему массив matrix в качестве аргумента.
    }

    private static void print(int[][] matrix) { /// Этот код содержит метод print, который принимает в качестве аргумента двумерный массив целых чисел matrix. Метод выводит этот массив в консоль.
        for (int[] row : matrix) { /// Для вывода двумерного массива используется цикл for-each для перебора каждой строки в массиве
            System.out.println(Arrays.toString(row)); /// Для вывода элементов каждой строки в одной строке используется метод Arrays.toString(), который возвращает строку, содержащую элементы массива, разделенные запятыми и заключенные в квадратные скобки.
        }
    }

    private static void initialize(int[][] matrix) { /// - объявить открытый статический метод initialize, который принимает двумерный массив целых чисел matrix в качестве аргумента.
        int value = 1; /// объявить целочисленную переменную value и инициализировать ее значением 1.
        for (int i = 0; i < matrix.length; i++) { /// начать цикл, который проходит по всем строкам массива matrix.
            for (int j = 0; j < matrix[i].length; j++) { /// начать второй цикл, который проходит по всем столбцам массива matrix.
                matrix[i][j] = value; /// Каждый элемент массива устанавливается на значение value, которое изначально равно 1
                value++; /// Затем value увеличивается на 1
            }
        }
    }

    public static void shuffle(int[][] m) {

    }
}


///В этом примере у нас есть три метода: инициализация, печать и перемешивание. Метод initialize заполняет двумерный массив int значениями, начинающимися с 1 и увеличивающимися на 1 для каждого элемента. Метод print печатает содержимое двумерного массива int в отформатированном виде. Метод shuffle перемешивает строки двумерного массива int в соответствии с представленной ранее реализацией.
//
//В основном методе мы сначала предлагаем пользователю ввести количество строк и столбцов для матрицы. Затем мы создаем новый двумерный массив int с указанным количеством строк и столбцов и инициализируем его значения с помощью метода initialize. Затем мы печатаем исходную матрицу, используя метод печати.
//
//Далее мы перемешиваем строки матрицы, используя метод перемешивания, и печатаем перетасованную матрицу, используя метод печати.
